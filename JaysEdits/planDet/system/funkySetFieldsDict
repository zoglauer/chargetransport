FoamFile
{
    version         2.0;
    format          ascii;
    class           dictionary;
    object          funkySetFieldsDict;
}

//variables $defaultVariables;

expressions
(
	
//	for (int i = 0; i < 37; i++) {
	strips //value of strips set to constant, count with i
	{
//	float gapWidth = 0.00025; //meters, 0.25mm
//	float stripWidth = 0.00175; //meters, 1.75mm
//	float stripLength = 0.072; //meters, 72mm
		field phi;
		keepPatches 1;
		valuePatches: front;
		expression " pos().x > ( (0.00325 + 0.00175) * 0.0 + 0.00325 ) &&\
		pos().x < ( (0.00325 + 0.00175) * (0.0 + 1) )  &&\
		pos().y > 0.00325 &&\
		pos().y < ( 0.00325 + 0.072 ) &&\
		pos().z < 0.0009 ? 1000.0 : 0.0";
		//expression "1000.0";
	  keepPatches 1;

	}

//	}
/*
	runtwice //interStrips //value of in between strips boggs approx
	{
	keepPatches 1;
	field phi;
	expression "0.0";
	keepPatches 1; 
	}
*/
/*
	stripEdges edges of strips with guard ring? boggs approx
	{
	
	}

	interpol //interpolation in regions not fit by above (zerGradient?)
	{

	}
*/
	
);
